# P3 完整规划

> 📦 **历史归档**（2026-06-12 标注）：P3 已全部 ship（0.2.0/0.3.0/0.4.x → 0.5.0），
> 实施记录见 `docs/history/P3-*-shipped.md` + PROCESS.md 历史归档表。**§8 已作废**。
> 本文留作历史参考。

日期：2026-04-20
起点：P2 完成（10/10），仓库已完成 owl-ts → owl 迁移
交付目标：GUI `0.2.0`/`0.3.0` GitHub Releases + CLI `@orpheus-aviary/owl-cli` 通过 `npm i -g` 发包 + UX 完善 + MCP（可选）
（单二进制、installer 内置 CLI、`electron-updater` 均为 post-P3 候选，见 §10）

---

## 1. 阶段总览

| 阶段 | 交付物 | 依赖 |
|---|---|---|
| **P3.0** | ✅ 仓库 rename（已完成，2026-04-20） | 无 |
| **P3.0.5** | ✅ Pre-release 修复打磨（7/7 完成 2026-04-28，含 4 个 bug + AI 卡片批量审核/折叠 + 列表与快捷键打磨 + 跨页 stale-list bus 重构；#11 经查是缺失功能不是 regression，已挪到 P3.4） | P3.0 |
| **P3.1** | GUI `0.2.0` 首发（electron-builder + GitHub Releases） | P3.0.5 |
| **P3.2** | CLI 核心开发（命令集 + daemon 检测 + 直连 SQLite + schema migration + `open_note_in_gui` 反向通道） | P3.1（可并行） |
| **P3.3** | 统一发 `0.3.0`（GUI release + CLI npm publish，独立渠道） | P3.1 + P3.2 |
| **P3.4** | UX 完善（6 子项，排序已敲定 2026-05-04，见 §7） | P3.3 |
| **P4** | migration 同步 + 远程 daemon 连接（rclone bisync 替代） | P3.4 |
| **P5** | 打包 / 发布自动化（CI matrix / Win+Linux 构建 / electron-updater / 单二进制 / GUI↔CLI installer 联动） | P4 |
| **P6** | 次要功能集（tray 图标、图片粘贴、FIM 补全、`append_memo` 语义、AI banner option C）— 非核心，保留记录 | P5 |
| **P3.5（可选）** | MCP server `@orpheus-aviary/owl-mcp` | 任意时刻，独立 |

**不在 P3**：远程 daemon 连接、migration 同步 → 留给 P4。Semantic search / embeddings → post-P4（FTS 质量出问题再做）。打包自动化 / 多平台 / installer 联动 → P5（见 §10 候选项已全部收进 P5）。

---

## 2. 核心架构决策（P2 讨论落定）

### 2.1 CLI 与 GUI 对等独立

- CLI 通过 `npm i -g @orpheus-aviary/owl-cli` 单独安装
- GUI 通过 GitHub Releases 下载 installer
- 两者**互不拉起**
- **0.3.0 明确 GUI installer 不内置 CLI**；"Docker Desktop 式 installer 把 `owl` 复制到 PATH" 是 post-P3 候选（见 §10），此处不是 P3 架构承诺

### 2.2 Daemon 归属

- GUI 启动时自动 spawn daemon（已有行为）
- **GUI 退出 ≡ 停自己拉起的 daemon**（2026-04-28 P3.1 设计落定，见 `docs/plans/2026-04-28-p3-1-gui-0.2.0-release-design.md` §2.2）：
  - 红叉关窗：隐藏窗口，app 保留在 dock，daemon 继续跑
  - Cmd+Q / 菜单 Quit：完全退出；若 daemon 是本次 GUI 启动时自己拉起的（`daemonStartedByGui=true`）→ SIGTERM + 3s 超时 SIGKILL + 清 pid，再退 GUI；若 daemon 在 GUI 启动前已存在 → 只退 GUI，不碰 daemon
- CLI **永不自动拉 daemon**
  - GUI 在跑 → CLI 走 HTTP
  - GUI 未跑 → CLI 直连 SQLite（WAL 模式短事务）
  - CLI 若需要"常驻 daemon"场景，命令名与启动语义留待 P3.2 CLI 设计时再定（0.2.0 scope 内无 CLI）
- WAL 允许并发读 + 单写，SQLITE_BUSY 时 CLI 指数退避重试 3 次

### 2.3 没有"CLI 下载 GUI"命令

- npm 不分发 200MB 二进制
- `owl doctor` 检测环境，打印 GitHub Releases URL 供用户手动下载

### 2.4 版本命名

| 阶段完成点 | 版本 | 语义 |
|---|---|---|
| P2 完成（GUI 基础功能） | `0.2.0` | 首个有 release 的 milestone |
| P3 完成（CLI + GUI 全套） | `0.3.0` | 外部调用可用 |
| P4 完成（同步稳定） | `1.0.0` | API 契约稳定 |

用 `0.x` 起步的理由：P3 / P4 还会引入显著变动（CLI 新增公共面、P4 schema 改动），`0.x` 的 minor bump 允许 breaking，不消耗 major。

### 2.5 npm scope

- 组织名：`@orpheus-aviary`（用户需在 npm 申请免费组织）
- 命名格式：`@orpheus-aviary/<project>-<variant>`
- **P3 只 rename 对外发布的包**：`@owl/cli` → `@orpheus-aviary/owl-cli`
- **内部 workspace 包保持 `@owl/*`**：`@owl/core`、`@owl/daemon`、`@owl/gui` 不改名
  - 理由：内部名字不暴露给用户；全量 rename 会把大量机械改动混进 P3 关键路径
  - 未来若需要公开 core，再 rename 成本低
- `@owl/core` **不发布**，通过 bundling 合并进 CLI（见 §5.3）
- 未来 `@orpheus-aviary/lark-cli`、`@orpheus-aviary/jay-cli` 等无缝扩展

### 2.6 打包不签名

- macOS：不上 Apple Developer（$99/年），用户右键 → 打开绕过 Gatekeeper
- Windows：不买 code signing 证书（$100-400/年），用户点 "仍要运行" 绕过 SmartScreen
- Linux AppImage：天然不需要签名
- 等有真实用户量抱怨再补签名

---

## 3. P3.0.5 — Pre-release 修复打磨

日期：2026-04-28（**完成**）
目标：清掉 0.2.0 首发面前的所有 P0/P1 问题。**全部完成（含手动测试）后才进 §4 P3.1 打包**。

**完成快照（2026-04-28）**：7/7 项 + 跨页 stale-list bus 重构。228/228 测试绿。详见 `PROCESS.md` "P3.0.5" 段落 commit 表。下一步 §4 P3.1。

### 3.1 范围

7 项，分三批；每批内顺序可调，跨批顺序保持。

**Batch 1 — Regression / 阻塞性 bug（最先做）**

| 项 | 描述 | 排查要点 |
|---|---|---|
| #10 | 拖动到"未分类"区域无法 drop | `UnfiledSection`（FolderPanel.tsx:325）只是 div+button，没用 `useDroppable`；面板底部 `RootBlankDrop` 能 drop，展开的"未分类"section 自己不接收 |
| #4 | 从回收站恢复后文件夹树未刷新 | 缺 invalidation 或事件驱动；NoteListStore / FolderStore 同步 |

> **#11 已移出**（2026-04-28 复核）：经代码审查 `dnd-types.ts` + P2-5c 设计文档（`docs/plans/2026-04-15-p2-5c-dnd-design.md` §1），笔记同层级排序**从未实现**（笔记无 `position` 字段，P2-5c 显式排除）。`App.tsx:166` 的 `handleNoteDrop` 也写明 "gaps not a valid target for notes"。该项不是 regression，是缺失功能，挪入 §7 P3.4 单独 brainstorm（需要 schema 改动 + daemon API + GUI gap drop target）。

**Batch 2 — 编辑器视觉与命令打磨**

| 项 | 描述 | 排查要点 |
|---|---|---|
| #9 | 滚动条区域背景白色（dark theme） | scrollbar gutter / track 没继承主题；CodeMirror + 全局滚动样式都查 |
| #6 | 有序/无序列表后续行内容颜色（黄→白） | CodeMirror 高亮规则把列表项 marker 与正文一同着色，需把 body 改回 default fg |
| #7 | 选中已加粗文本再按 Cmd+B 应取消加粗（Bold/Italic/InlineCode 全部 toggle 语义） | 复用 CM6 markdown command 而非手写 wrap |

**Batch 3 — AI 能力修复（最大块，单独一批）**

#### #3 AI thinking 协议修复（含 GUI 显示与设置 toggle）

**背景**（2026-04-28 调研落定）：

| 模型 | 流式响应 | 多轮回带 |
|---|---|---|
| DeepSeek V4 Pro/Flash（用户当前 config） | `delta.reasoning_content` (string) | **必须回带** assistant message 的 `reasoning_content` 字段 → 不带 = 400 |
| DeepSeek V3 reasoner / R1（旧） | 同上 | 必须丢弃 → 带了 = 400 |
| Anthropic Extended Thinking | `thinking_delta` + `signature_delta` 事件 | 必须回带 thinking block **含 signature** → 缺 signature = 400 |
| OpenAI o-series（chat completions） | 不暴露 | 无需处理 |

当前 `packages/daemon/src/ai/llm-client.ts` 全文不处理 thinking / reasoning，导致 V4 / Anthropic Extended Thinking 默认 400。

**修复范围**（一次性把 OpenAI 与 Anthropic 双 adapter 都补齐）：

1. **类型层**（`llm-client.ts`）：
   - `LlmContentBlock.type` 增加 `'thinking'`，可选 `signature?: string`
   - `StreamChunk` 新增 `{ type: 'thinking_delta'; text: string }` 与 `{ type: 'thinking_signature'; signature: string }`
2. **OpenAI adapter**：
   - 流式：`delta.reasoning_content` → `thinking_delta`
   - `toOpenAiMessage`：assistant 消息附带 `reasoning_content` 字段（仅当 `thinking_round_trip = true` 且本轮捕获到 thinking）
3. **Anthropic adapter**：
   - 流式：`content_block_delta` 的 `thinking_delta` / `signature_delta`
   - `buildAssistantMessage`：把 thinking block（含 signature）放在 content blocks 头部（仅当 `thinking_round_trip = true`）
4. **agent-loop**：累积流式 `thinking_delta` 到本轮 assistant message；保留 signature
5. **conversations**：在 LlmMessage 上持久化 thinking 文本与 signature
6. **daemon SSE**：把 `thinking_delta` 透传给 GUI（沿用现有 `dispatcher` 事件契约）
7. **GUI**：
   - `ai-store` / `ai-dispatcher` 处理 `thinking_delta` 事件
   - `MessageBubble` 渲染**可折叠** thinking 块（默认折叠）
8. **配置**（`packages/core/src/config/index.ts`）：
   - 新增 `[llm].thinking_round_trip: bool`，默认 `true`（V4 / Anthropic 趋势）
   - 用户用 V3 reasoner / OpenAI o-series chat 时手动改 `false`
9. **GUI 设置页**：在 LLM 栏（`SettingsPage` 自定义 tab）加一个 toggle "Round-trip thinking content"，与 url / model / api_key 同行块

**手动复现**：用现有 `deepseek-v4-pro` config（thinking 默认开），第 1 轮 prompt 含工具调用，让 AI 进入 multi-turn → 修复前 400，修复后正常 + GUI 折叠块显示 thinking 文本。

#### #2 AI 对话卡片 UX（批量同意 + 单卡同意 + 折叠）

| 子项 | 设计 |
|---|---|
| 单卡 / 批量"同意" | **复用 P2-8 Tier-1 自动合并路径**（即 `applyNoteAppliedFromAi` + 全局 toast，**不打开编辑器 tab**） |
| 单卡"打开"（已有按钮） | 维持 P2-8 行为：开 editor + staging + ConflictDialog 流程 |
| 编辑笔记卡片折叠 | 默认展开；点击 chevron 折叠；"打开" / "同意 / 自动合并完成" 后自动折叠 |
| 批量按钮位置 | MessageList footer 右上区，仅当本回合存在 ≥2 张未处理草稿卡时显示 |
| 双击防抖 | 按钮 in-flight 期间 disabled |
| Per-card 失败隔离 | 每张卡 try/catch；3 张中 1 张失败 → 2 张成功折叠、1 张红色错误状态 + retry 按钮 |
| 冲突处理 | 单卡 Tier-1 路径已包含冲突检测；批量同样；冲突卡保持展开并标黄 |

### 3.2 顺序考量

- Batch 1 → Batch 2 → Batch 3：先清回归，再做编辑器视觉/命令打磨，最后做最大块的 AI 能力修复
- Batch 2 内 #6 / #9 / #7 共享 CodeMirror 改动面，可顺手做完
- Batch 3 内 #3 在 #2 之前：#3 是协议级修复，影响 daemon `llm-client` / `agent-loop` / `conversations` / `sse` 与 GUI `ai-store` / `ai-dispatcher` / `MessageBubble` / `SettingsPage` / `core/config`，先稳定基线再做 #2 的 UX 增强

### 3.3 测试责任

| 项 | 测试要求 | release-blocking gate |
|---|---|---|
| #10 | store 单测 + **手动测试清单**（拖入"未分类"必须 drop 成功并立即可见） | 手动 |
| #4 | 手动测试：单笔记恢复 / 批量恢复 / 含子文件夹恢复，三种路径文件夹树均刷新 | 手动 |
| #9 | 手动：dark theme 下所有滚动框（编辑器、笔记列表、AI 对话、文件夹树、Browser、Trash、Reminders）gutter 颜色一致 | 手动 |
| #6 | 手动：有序 / 无序 / 嵌套列表后续文本颜色与正文一致；单测可补 CodeMirror highlight token snapshot | 手动 |
| #7 | 编辑器键盘命令单测（jsdom 范围内） + 手动：连按两次 Cmd+B / Cmd+I / Cmd+E 均还原原文 | 自动 + 手动 |
| #3 | **必须自动化**：(a) OpenAI adapter — `delta.reasoning_content` 流转换为 `thinking_delta` chunk；(b) OpenAI adapter — `thinking_round_trip=true` 时 assistant message 含 `reasoning_content`，`false` 时不含；(c) Anthropic adapter — thinking + signature 事件流处理 + 回带 thinking block。**手动复现**：本机 deepseek-v4-pro config 跑 multi-turn + tool call，验证不再 400 + GUI 折叠块显示 thinking | 自动 + 手动 |
| #2 | ai-store 单测：(a) 批量同意路径走 Tier-1 而非 staging；(b) 3 张卡 1 失败 → 2 成功 + 1 错误状态；(c) 冲突卡保持展开并标黄；(d) in-flight 按钮 disabled 防双击。手动：批量改标签场景一键通过 | 自动 + 手动 |

**通用规则**：

- 标"手动"的项：必须输出**步骤化、可复现**的手动测试清单（参考 owl `CLAUDE.md` 手动测试规范），由用户最终签字才能进 §4
- DnD 类（#10）项目目前无 renderer 端 E2E 设施，故走"手动 gate + store 单测保护事件契约"，**不在本批引入 playwright**

### 3.4 完成定义

- 7 项全 ✅
- `just check`（lint + typecheck）通过
- `just test`（core + daemon + gui 全套 vitest）通过，含 #3 / #2 / #7 / #10 新增单测
- 每项的手动测试清单按 owl `CLAUDE.md` 规范输出并经用户签字
- 用户最终确认体感后，进 §4 P3.1 打包

### 3.5 不在本阶段处理（明确推迟到 §7 P3.4）

| 项 | 描述 | 推迟理由 |
|---|---|---|
| #5 | 笔记名 VSCode 风格 tab（斜体预览 / 双击编辑 / 单击切换关闭其它预览） | 体感优化，不阻塞首发；tab 模型涉及编辑器多 tab 状态机，单独设计 |
| #1 | 图片复制粘贴 + 缓存目录 + 自动链接 | 涉及存储路径（`attachments/<note-id>/?`）、Markdown 链接格式、migration 同步范围、回收站清理。实现前单独 brainstorm，本阶段只记录 |
| #8 | FIM 上下文补全 + Tab 补全 | 独立大功能（编辑器 LLM 集成、context 选取、UI 反馈），单独 spec |

---

## 4. P3.1 — GUI 0.2.0 首发

> 详细设计：`docs/plans/2026-04-28-p3-1-gui-0.2.0-release-design.md`

### 4.1 范围

**0.2.0 仅 macOS arm64**。Windows / Linux 构建 + GitHub Actions CI 延后到下一个 minor（0.2.1 或 0.3.0 的一部分）。本机 `pnpm --filter @owl/gui package` 出 dmg，`gh release create v0.2.0` 手动上传。

Tag 命名：`v0.2.0`（GUI-only tag namespace）。CLI 未来另用 `cli-v*`（方案待 P3.3 前 brainstorm，见 §10 tag 策略）。

### 4.2 技术要点

- `packages/gui/electron-builder.yml`：`mac.target=dmg[arm64]`，`identity: null`（不签名），`asar: false`（见下）
- daemon 子进程用 **Electron-as-Node**（`process.execPath + ELECTRON_RUN_AS_NODE=1`）代替 `spawn('node', …)` — packaged app 不依赖用户机器有 node；env 必须 `...process.env` 展开父环境，否则 daemon 丢 `HOME` / `PATH` / `HTTPS_PROXY` / `ANTHROPIC_API_KEY` 等关键变量
- `asar: false` 选择（2026-04-28 实测落定）：pnpm workspace symlink + electron-builder asarUnpack 过滤器冲突（`packages/core/dist/*` 不在 `packages/gui/` 下 → 报错）；`.npmrc: node-linker=hoisted` 无效（workspace 包仍然是 symlink）。`asar: false` 代价：dmg 大一点、冷启动略慢。可在 0.2.1 用 `pnpm deploy` 产 flat closure 改回 asar
- daemon CLI 必须 `program.parse(process.argv, { from: 'node' })`：Electron-as-Node 下 `process.versions.electron` 为真，commander 默认走 `from: 'electron'` 只 strip argv[0]，script path 被误读为 subcommand
- `postinstall: electron-builder install-app-deps` **不设**（会让 `.pnpm/better-sqlite3` rebuild 为 Electron ABI 132 → 所有 `just test` 段错误）；`install-app-deps` 挪进 `pnpm package` 脚本内，只在打包时跑
- Windows / Linux / CI workflow → 见 §4.x 延后项

### 4.3 实施步骤

实际落地（2026-04-28 smoke 1/2/3 绿）：

1. `pnpm add -D electron-builder@^25.1.8` 到 `@owl/gui`
2. `packages/gui/electron-builder.yml` + `resources/`（源图 `owl-logo-original.png`）
3. `packages/gui/scripts/build-icons.mjs`（sips + iconutil 生成 `icon.icns`，gitignore）
4. 改造 `packages/gui/src/main/daemon.ts`：Electron-as-Node spawn + `daemonStartedByGui` 标记 + `stopDaemonGracefully`（SIGTERM → 3s → SIGKILL，只在自己拥有时执行）
5. 改造 `packages/gui/src/main/index.ts`：红叉 `hide()` + `activate` 优先 show 已有窗口 + `before-quit` await `stopDaemonGracefully`
6. 改 `packages/daemon/src/cli.ts`：`program.parse(process.argv, { from: 'node' })`
7. 根 `.npmrc: node-linker=hoisted`（pnpm + electron-builder 兼容必备）
8. 根 `README.md`（极简 + 中文下载说明）
9. `justfile`：`just package` + `just unpackage`（后者恢复 Node ABI 以便再跑测试）
10. `pnpm --filter @owl/gui package` → `release/Owl-0.2.0-arm64.dmg`（~129 MB）
11. `gh release create v0.2.0` 挂 dmg + SHA256

### 4.4 图标与资源

- macOS `.icns`：源图 `packages/gui/resources/owl-logo-original.png`（1254×1254 RGBA），`scripts/build-icons.mjs` 用 `sips` + `iconutil` 生成；产物 gitignore
- Windows `.ico` / Linux `.png`：推迟到 win/linux 构建时再做

### 4.x 延后项（本轮移出 P3.1）

| 项 | 归属 |
|---|---|
| Windows 构建（`nsis`） | 0.2.1 或 0.3.0 |
| Linux 构建（`AppImage`） | 同上 |
| GitHub Actions `release-gui.yml`（macos + windows + ubuntu 矩阵） | 同上 |
| codesign / notarize（macOS Apple Developer） | post-P3 |
| `electron-updater` 自动更新 | post-P3 |
| macOS universal（x64 + arm64） | 按需 |
| macOS menubar tray 图标 + 设置 toggle | P3.4（见 §7） |

---

## 5. P3.2 — CLI 核心开发

**状态（2026-05-04）**：§5.5 migration runner **已 ship**（commit `38e9243`，245/245 测试）。P3.2-b MigrationDialog **已 ship**（2026-04-30，271/271 测试）。P3.2-c CLI 核心 **已 ship**（2026-05-02，9 commits `10b8bd5`..`7732efc`，404/404 测试）。P3.2-d SSE 反向通道 **已 ship**（2026-05-03，5 commits `5168b60`..`c565955`，426/426 测试）。**P3.2.5 release polish 已 ship**（2026-05-03..04，3 commits `e6ff4eb`..`b49f6da`，459/459 测试）：`owl skill export` 命令、GUI 高级设置 CLI 检测（含 PATH fallback + nvm semver）、退出时未保存 tab 弹窗（含 AI 冲突 handoff / 10s timeout / Migration 模式跳过）。**P3.3 0.3.0 已 ship**（2026-05-04，2 commits `08e9965`..`fabf142`，461/461 测试）：GUI `v0.3.0` GitHub Release + CLI `cli-v0.3.0` / `@orpheus-aviary/owl-cli@0.3.0` npm publish，独立渠道同日发版；顺带修复 `npm i -g` symlink 下 entry guard 失效 + `program.version()` 硬编码脱节，加 spawn-via-symlink smoke 测试防回归。

- ✅ **P3.2-a** migration runner（§5.5）
- ✅ **P3.2-b** GUI MigrationDialog（`whenReady` precheck + 4 屏状态机 + onProgress 4-phase emit）
- ✅ **P3.2-c** CLI 核心（§5.1-5.4 全部；13 commands + tsup bundle + publishable manifest）
- ✅ **P3.2-d** SSE `/events` 反向通道 + `owl open`（shipped 2026-05-03，5 commits `5168b60`..`c565955`，426/426 测试 + 端到端 smoke 通过；设计 `docs/plans/2026-05-02-p3-2-d-events-channel-design.md`）

### 5.1 apps/cli 目录结构

```
apps/cli/
├── src/
│   ├── index.ts              # commander 入口
│   ├── commands/
│   │   ├── search.ts
│   │   ├── get.ts
│   │   ├── create.ts
│   │   ├── edit.ts
│   │   ├── memo.ts
│   │   ├── todo.ts
│   │   ├── remind.ts
│   │   ├── open.ts           # 打开编辑器 tab（需 reverse channel）
│   │   └── doctor.ts         # 环境诊断
│   ├── modes/
│   │   ├── http.ts           # 调 daemon 的实现
│   │   └── direct.ts         # 直连 @owl/core 的实现
│   └── lib/
│       ├── daemon-detect.ts  # HTTP /status 心跳
│       └── db-lock.ts        # SQLITE_BUSY 重试
├── package.json              # "@orpheus-aviary/owl-cli"
└── tsconfig.json
```

### 5.2 智能切换

```typescript
const daemonAlive = await detectDaemon();  // HTTP /status, 200ms timeout
const mode = daemonAlive ? 'http' : 'direct';
```

- `http` 模式：所有命令走 `fetch` 到 `http://localhost:<daemon.port>`；端口从 `owl_config.toml` 的 `daemon.port` 读取，默认 `47010`（见 `packages/core/src/config/index.ts:111`）。**禁止硬编码 `8080` 或任何其他数字字面量**
- `direct` 模式：直接 `createDatabase()`（来自 `@owl/core`），执行完关闭
- 环境感知：`http` 模式暴露 `open` 等需要 GUI 的命令；`direct` 模式隐藏
- `detectDaemon()` 同样用配置 `daemon.port`，不是猜端口

### 5.3 打包与发布（决定：tsup bundle + npm i -g）

**0.3.0 分发方式定死 `npm i -g @orpheus-aviary/owl-cli`**。单二进制（`pkg`）推迟到 post-P3（见 §10）。

**关键问题**：CLI 当前依赖 `@owl/core`（`workspace:*`、`private`），直接 `npm publish` 会爆。采用 **选项 B：bundle core 业务逻辑、external native modules、发布时输出干净 manifest**。

**实施步骤**：

1. 引入 `tsup` 作为 CLI 构建工具（esbuild 内核，零配置 ESM + d.ts）
2. `apps/cli/tsup.config.ts`：
   - `entry: ['src/index.ts']`
   - `format: ['esm']`（Node 20+ 原生支持）
   - `bundle: true`、`noExternal: ['@owl/core']` — 把 core 业务逻辑打进 CLI 产物
   - `external: ['better-sqlite3']` — native，必须运行时解析
   - `external: ['drizzle-orm', 'commander', 'pino', 'pino-roll', 'uuid', 'smol-toml']` — 纯 JS，也外置，保持体积小和版本透明
3. **生成 publishable manifest**（与 workspace 的 `package.json` 不同）：
   - `name: "@orpheus-aviary/owl-cli"`、`private: false`
   - 移除 `@owl/core` 的 `workspace:*` 依赖
   - `dependencies` 列出所有 external：`better-sqlite3`、`drizzle-orm`、`commander`、`pino`、`pino-roll`、`uuid`、`smol-toml`
   - `bin: { "owl": "dist/index.js" }`（bin 名从 `owl-cli` 改为 `owl`）
   - 实现：prepack 脚本生成 `dist/package.json`，`pnpm publish` 指向 `dist/`；或用 `clean-publish`
4. CI 发布：
   ```bash
   pnpm --filter @owl/cli build
   pnpm --filter @owl/cli publish --access public --no-git-checks
   ```

**native module UX 风险**：

- `better-sqlite3` 对主流 Node LTS 有 prebuilt binary；非 LTS / musl / 非主流架构会落到本地编译（需要 python + make + C toolchain）
- `owl doctor` 必须检测 Node 版本 + `better-sqlite3` 能否加载，给明确错误提示和引导

### 5.4 `open_note_in_gui` 反向通道

CLI 在 http 模式想让 GUI 打开指定笔记 tab，需要 daemon → GUI 推送。

**方案对比**：

| 方案 | 复杂度 | 优点 | 缺点 |
|---|---|---|---|
| WebSocket (`@fastify/websocket`) | 中 | 全双工，未来扩展性好 | 多一个协议 |
| Server-Sent Events（已有） | 低 | AI 已用 SSE，复用基础设施 | 单向（daemon → GUI） |
| 轮询 `/events?since=<cursor>` | 低 | 无状态 | 延迟高 |

**推荐 SSE**：已有 `initSse` 基础设施，GUI 侧加一个全局事件订阅组件。路由 `/events`：
- 推送事件类型：`open_note`、未来扩展 `config_changed`、`note_applied_external` 等
- GUI mount 时订阅，收到 `open_note { id }` 调 `openNoteById(id) + navigate('/')`

### 5.5 Schema migration 系统（决定：rebuild + backup，user_version 单一 truth source）

**状态（2026-04-30）**：P3.2-a + P3.2-b 均已 ship。P3.2-a 设计文档 `docs/plans/2026-04-29-p3-2-a-migration-runner-design.md`（v5，5 轮审查）；代码 commit `38e9243`；T1-T15 + backup 共 17 个测试覆盖；真库 52 笔记迁移 smoke 通过。P3.2-b 设计文档 `docs/plans/2026-04-30-p3-2-b-migration-dialog-design.md`；代码 commit `e302838`；新增 26 测试（总 271）；真库 smoke S1-S8 全过。本节以下内容保留作历史参考与未来 forward migration 的契约源。

**核心决策**：0.3.0 对老库采取"rebuild + 备份 + 不兼容旧架构"策略，避免 baseline 检测的复杂度和 DDL / ALTER / runner 三套并存的维护负担。

**目录结构**：

```
packages/core/src/db/migrations/
├── 0001_initial.sql   # = 今天完整 DDL（含 auto_delete_at、FTS 虚表、触发器）
├── 0002_*.sql         # 未来 schema 变更
└── ...
```

**单一 truth source = `PRAGMA user_version`**。不引入 `schema_version` 表。

**Runner 逻辑（启动时）**：

代码里维护一个常量 `LATEST_KNOWN_VERSION`（0.3.0 = 1，未来版本 bump）。判断顺序**必须先处理未来版本**，否则 `v >= 2` 会把未知版本误吞：

```
const LATEST = LATEST_KNOWN_VERSION;  // 0.3.0 中为 1
读 PRAGMA user_version as v:

  if v > LATEST:
    → 报错："数据库 v{v} 来自更新版本应用（本版本支持到 v{LATEST}），请升级应用"，拒绝打开

  if v == 0:
    if 库文件不存在 或 sqlite_master 无业务表:
      → 新库：跑 0001_initial.sql，设 user_version = LATEST（= 1）
    else:
      → 老库（0.2 或更早）：走一次性 rebuild 分支（见下）
    return

  if v == LATEST:
    → 正常启动（0.3.0 下 v == 1）
    return

  if v < LATEST:
    → 按顺序 apply version v+1 ... LATEST 的 migration 文件（未来版本用）
    return
```

0.3.0 里唯一会走"apply migrations 前进"的场景是**没有**的（`v == 1 == LATEST`）；该分支为未来版本预留。但代码结构先写好，避免 0.4.0 再改 runner 骨架。

**一次性 rebuild 分支**（仅 0.3.0 ship，0.4.0 可删）：

> ⚠️ WAL 模式下 `owl.db-wal` / `owl.db-shm` 是活跃 sidecar，简单 `cp owl.db ...` 会漏掉已提交但未 checkpoint 的数据。必须用 **SQLite online backup API** 或显式 checkpoint，且替换阶段必须清掉旧 sidecar。

1. **确认独占访问**：检查没有其他进程持有 `owl.db`（daemon 未起、无残留 `.owl.lock`）。有冲突 → 中止并报错
2. **打开旧库 + WAL checkpoint**：`PRAGMA wal_checkpoint(TRUNCATE)` — 把 WAL 内容合并回主文件、清空 sidecar
3. **备份**：用 better-sqlite3 的 `db.backup('owl.db.v0.2-backup-<unix-ts>')`（在线 backup API，自动处理一致性快照）
4. **新建 `owl.db.new`**（独立路径），跑 `0001_initial.sql`
5. **按表顺序从旧库 INSERT 到新库**（清单见下，用事务包起来）
6. **重建 FTS**：`INSERT INTO notes_fts(notes_fts) VALUES('rebuild')`
7. **新库 `PRAGMA user_version = 1`**，关闭新库连接（确保 WAL flush）
8. **关闭旧库连接**
9. **Sidecar 清理 + 原子替换（带回滚）**：
   ```
   try:
     a. fs.renameSync('owl.db', 'owl.db.old-pre-v0.3')
     b. try fs.unlinkSync('owl.db-wal'); try fs.unlinkSync('owl.db-shm')  // ENOENT 忽略
     c. fs.renameSync('owl.db.new', 'owl.db')
   catch err:
     // 回滚：把原库放回原位
     if exists('owl.db.old-pre-v0.3') and not exists('owl.db'):
       fs.renameSync('owl.db.old-pre-v0.3', 'owl.db')
     throw err
   ```
   - 步骤 2 已经 `wal_checkpoint(TRUNCATE)`，主文件自包含；删掉 `-wal`/`-shm` 不会丢数据，SQLite 下次打开自行重建
   - 回滚后原路径恢复原库；`.new` 和 `.backup` 留在盘上供 debug

10. **失败恢复契约（精确版）**：
    - **步骤 1-8 任一失败**：原库路径 `owl.db` 原样不动；`.new` / `.backup` 可能部分存在，清理后重试
    - **步骤 9c 失败**：try/catch 自动回滚，最终原路径仍指向可用原库
    - **进程被 `kill -9` 或断电发生在步骤 9a 与 9c 之间**（极小窗口，非应用层错误）：原路径可能不存在，但数据仍在 `owl.db.old-pre-v0.3` 和 `owl.db.v0.2-backup-<ts>`；启动时应检测"`owl.db` 不存在但 `.old-pre-v0.3` 存在"这种残局，log 指引用户手动 rename 回（或未来 `owl doctor --recover` 自动处理）
    - **任何情况下**：`owl.db.v0.2-backup-<ts>` 始终是步骤 3 的一致快照，是最后保险

**数据迁移表清单（COPY）**：

- `folders`、`notes`（含 `auto_delete_at`）、`tags`、`note_tags`、`reminder_status`、`local_metadata`
- 实施时 `grep -r "CREATE TABLE" packages/core/src/db/` 再核对一次，防漏表

**不 COPY**：

- FTS 虚表 / 其镜像表（由步骤 4 重建）
- 触发器（`0001_initial.sql` 已定义）
- `owl.sync.db`（migration 同步副本，由 migration 工具管理，应用不碰）

**UX 与启动时序（关键：当前 GUI 在窗口创建前就 spawn daemon、daemon 启动即打开库）**

当前 `packages/gui/src/main/index.ts:59-71` 是 `whenReady → spawnDaemon → createWindow`，`packages/daemon/src/cli.ts:44` 启动即 `createDatabase()`。如果不动这个时序，没地方给用户弹 migration 确认框。**责任划分必须写死**：

- **GUI main process**：在 `whenReady` 内、**spawn daemon 之前**做 migration precheck
  1. 只读打开 `owl.db` → 读 `PRAGMA user_version`
  2. `v == 0` 且库非空（老库）→ 先 `createWindow`，前端加载一个专用 `MigrationDialog` 路由（不挂主 UI），等待用户确认
  3. 确认后：在 main 进程里同步跑 rebuild（better-sqlite3 同步 API，完成前**绝不 spawn daemon**）
  4. 完成后 → `spawnDaemon`，前端切到主 UI
  5. 用户取消 / rebuild 失败 → 显示错误界面 + 原库和 backup 路径，退出或重试
- **Daemon 独立启动**（无 GUI，例如 `just dev-daemon` 或用户直接跑）：启动时只读检查 `user_version`，若需要迁移 → **不调用 `createDatabase()` 的初始化路径**，log 清晰错误 + exit non-zero，提示用户启动 GUI 或跑 `owl migrate`
- **CLI `direct` 模式**：同 daemon — 检查，不自动迁移，提示用户跑 `owl migrate` 或启动 GUI
- **新增 `owl migrate` 子命令**：交互式 rebuild（TTY prompt y/N，`--yes` 跳过），供纯 CLI / 脚本场景使用

**关键不变量（按 runner 分支精确区分，不能一刀切"不是 LATEST 就抛"）**：

`createDatabase()` 内部流程：

1. 打开连接 → 读 `user_version as v`
2. **`v > LATEST_KNOWN_VERSION`** → 抛 `IncompatibleDbError`（数据库来自更新版本，应用无力处理。调用方退出 / 显示错误页）
3. **`v == 0` 且库非空（老库）** → 抛 `MigrationRequiredError`（需要用户确认的 rebuild。GUI 走 modal；daemon/CLI `direct` 打印引导后退出；CLI 可经由 `owl migrate` 进入）
4. **`0 < v < LATEST_KNOWN_VERSION`**（未来版本的常规向前迁移）→ **静默** apply 缺失的 migration 文件（纯增量 DDL / ALTER），完成后进入正常路径。**不抛 `MigrationRequiredError`**
5. **`v == LATEST_KNOWN_VERSION`** 或 **`v == 0` 且库为空**（初始化 0001）→ 正常路径

两种错误语义不同，不能合并：

| 错误 | 触发条件 | 处理方 | 可恢复性 |
|---|---|---|---|
| `MigrationRequiredError` | `v == 0` 老库（需破坏性 rebuild） | GUI modal / `owl migrate` | 用户确认后可恢复 |
| `IncompatibleDbError` | `v > LATEST`（未来库） | 退出 + 提示升级应用 | 非本版本能恢复 |

如果未来某个 migration 确实是破坏性的（例如 0.5.0 要重建某张表）、需要用户确认，再在该 migration 文件头标记 `requires_confirmation: true`，runner 读到时抛 `MigrationRequiredError`。当前（0.3.0）无此场景。

**备份命名**：`owl.db.v0.2-backup-<unix-ts>`

**失败恢复策略**：不自动还原；报错明确指出原库位置 + backup 位置，引导用户手动处理或联系。未来可考虑 `owl doctor --recover`（见 §10）。

**代码位置**：

- `packages/core/src/db/migrate.ts`（新建）：rebuild 主流程（checkpoint / backup API / copy / sidecar 清理 / 回滚）+ `MigrationRequiredError` + `IncompatibleDbError` + 未来常规前向 migration runner
- `packages/core/src/db/index.ts` 的 `createDatabase()`：打开后读 `user_version` 并按上文"关键不变量"的分支分派（`v > LATEST` 抛 `IncompatibleDbError`；`v == 0` 老库抛 `MigrationRequiredError`；`0 < v < LATEST` 静默前向迁移；其余正常）
- `packages/gui/src/main/index.ts`：`whenReady` 分支加 precheck，替换当前无条件 `spawnDaemon` 逻辑
- `packages/daemon/src/cli.ts`：启动时 catch `MigrationRequiredError`，log + exit 1
- `apps/cli/src/commands/migrate.ts`（新增）：TTY 交互 rebuild
- `createTables()` 整块 DDL 收掉，改由 `0001_initial.sql` 承担；替换当前 `migrateSchema()` 的 ad-hoc ALTER 逻辑

**一次性 vs 永久**：

- rebuild 分支代码只在 0.3.0 存在
- 0.4.0 假设所有活库 `user_version >= 1`，删除 rebuild 分支
- 用户跨版本跳跃（0.2 → 0.4+）会触发 "v=0 但代码不识别" 报错，引导先装 0.3.0 过渡

---

## 6. P3.3 — 统一发 0.3.0

**状态（2026-05-04）**：**已 ship**。GUI `v0.3.0` Release + `Owl-0.3.0-arm64.dmg`（https://github.com/orpheus-aviary/owl/releases/tag/v0.3.0），CLI `cli-v0.3.0` + `@orpheus-aviary/owl-cli@0.3.0` on npm。本地手动发布（CI workflows 延后至 0.3.1 或 post-P3），Tag 策略落地为 `v* = GUI` / `cli-v* = CLI`（见 §9.3 决议）。发包前发现 `npm i -g` 装的 CLI 入口 guard 对 symlink 失效（argv[1]=symlink vs import.meta.url=realpath），以及 `program.version()` 被硬编码成 `0.3.0-dev` 与 package.json 脱节；修在 `08e9965`，`realpathSync(argv[1]) === fileURLToPath(import.meta.url)` 比较 + 运行时读 package.json 版本号 + 新增 spawn-via-symlink smoke test（`apps/cli/src/bin.spawn.test.ts`，CLI 测试 117 → 119）。

### 6.1 版本定义

**0.3.0 ≔ GUI 通过 GitHub Releases 安装 + CLI 通过 `npm i -g @orpheus-aviary/owl-cli` 安装 + 两者独立工作 + daemon 存在时 CLI 走 HTTP、不在时 CLI 直连 SQLite（WAL）+ P3.2 的一次性库 rebuild 迁移**

**不包含**（移至 post-P3，见 §10）：

- GUI installer 内置 / 复制 CLI 到 PATH
- 单二进制（`pkg` / `@yao-pkg/pkg`）分发
- `electron-updater` 自动更新（首发手动替换）

### 6.2 CLI 发布流程

见 §5.3 的 tsup + publishable manifest 方案。命令：

```bash
pnpm --filter @owl/cli build
pnpm --filter @owl/cli publish --access public --no-git-checks
```

### 6.3 GitHub Release + npm 同步

`v0.3.0` tag 触发两条**独立**流水线（任一失败不阻塞另一条）：

- `.github/workflows/release-gui.yml` → GitHub Releases 三平台 installer
- `.github/workflows/release-cli.yml` → `npm publish @orpheus-aviary/owl-cli`

### 6.4 发版前手动 checklist

- [ ] 本地库已通过 rebuild 分支迁移到 `user_version = 1`，笔记内容逐条校验一致
- [ ] 三平台 installer 本地各跑通一次
- [ ] `npm pack` 产物用 `npm i -g <tarball>` 能启动，`owl --version` 正常
- [ ] `owl doctor` 在无 daemon 环境下正确引导下载 GUI
- [ ] `#真实` 标签笔记另存一份独立备份（独立于 rebuild backup，纯手动保险）

---

## 7. P3.4 — UX 完善

日期：2026-05-04 敲定 scope；尚未开工。

独立于 P3.1-3，6 个子项按建议顺序排。子项内部动工前再开各自 design doc（沿用 P3.2-a/b/c/d 模式），此处只列 scope 与关键决策。

### 7.1 子项清单与执行顺序

| # | 子项 | 规模 | 依赖 |
|---|---|---|---|
| **P3.4-a** | 笔记排序模型（置顶 `pinned_at` + 同层级 DnD `position`） | 中 | 无（首验 forward migration runner） |
| **P3.4-b** | 特殊笔记视觉区分（`#随记` 蓝边框 / `#待办` 粉边框） | 小 | 无 |
| **P3.4-c** | AI chat 笔记 id → 可跳转标题（方案 A） | 小 | 无 |
| **P3.4-d** | 标签栏输入框 Tab/Enter 区分补全 | 小 | 无 |
| **P3.4-e** | 笔记 tab VSCode 风格（斜体预览 / 双击编辑 / 单击切换关闭其它预览） | 中 | 无 |
| **P3.4-f** | 聊天持久化 + 侧栏布局 | 中 | 无（按既有 plan） |

执行顺序：**-a → -b → -c → -d → -e → -f**。-a 放最前的理由：schema 改动（0.4.0 首个 forward migration），顺便验证 P3.2-a 预留的 `applyForwardMigrations` 骨架；其余按"小 → 大"排，大块 -f 放最后。

### 7.2 P3.4-a 笔记排序模型（合并原"置顶" + 原 #11 同层级 DnD）

**动机**：置顶和同层级拖拽排序都要动 `notes` 表 schema，一次 migration 解决，避免 0002 + 0003 连环 ALTER。

**Schema 改动（`0002_sorting.sql`）**：

```sql
ALTER TABLE notes ADD COLUMN pinned_at INTEGER;      -- NULL=未置顶；有值=置顶时间戳（ms）
ALTER TABLE notes ADD COLUMN position  REAL;         -- 同层级排序 key（fractional indexing）
CREATE INDEX idx_notes_pinned_position ON notes(pinned_at DESC, position ASC, updated_at DESC);
```

`position` 采用 fractional indexing（浮点中值插入）：新节点插入 A / B 之间时 `position = (A.position + B.position) / 2`，避免批量 reindex。精度耗尽时 lazily rebalance 整个分组。

**统一排序规则**（编辑页 NoteList + 浏览页共用）：

```sql
ORDER BY
  CASE WHEN pinned_at IS NULL THEN 1 ELSE 0 END,   -- 置顶组优先
  pinned_at DESC,                                   -- 置顶组内按置顶时间降序
  position ASC,                                     -- 再按手动排序 key
  updated_at DESC                                   -- position 并列时按修改时间降序
```

**daemon API 新增**：

- `PATCH /notes/:id/pin` → body `{pinned: bool}`，set/clear `pinned_at = Date.now()`
- `PATCH /notes/:id/position` → body `{before_id?, after_id?}`，计算 fractional position 并写入
- 现有 `GET /notes` 按上面 ORDER BY 返回（不改 API shape）

**GUI 行为**：

- 编辑页 NoteList + 浏览页右键菜单：置顶 / 取消置顶
- DnD：gap drop target（`@dnd-kit/sortable`）允许拖到同层级两笔记之间；跨组（置顶↔非置顶）拖动自动切换 `pinned_at`
- 搜索结果同样按上面规则排（置顶结果在上）

**范围内**：编辑页 NoteList + 浏览页 NoteList（两处都有右键置顶 + DnD gap target）
**范围外**：侧栏快捷按钮（用户 2026-05-04 明确不要："不喜欢就不置顶也不影响"）

### 7.3 P3.4-b 特殊笔记视觉区分

- `#随记`（id `...0001`）：NoteList / 浏览页列表项左侧加 **蓝色** 边框（4px 纯色）
- `#待办`（id `...0002`）：同上，**粉色** 边框
- 编辑器 tab 保持不变（避免脏标 / 激活态叠加颜色噪音，用户 2026-05-04 明确）
- 不加侧栏快捷按钮（同上）
- 颜色 token 走 `tailwind.config` 新增 `--owl-pin-memo: #3b82f6` / `--owl-pin-todo: #ec4899`，未来可主题化

### 7.4 P3.4-c AI chat 笔记 id → 可跳转标题

**方案 A（起步）**：GUI 侧 regex 后处理。

- `MessageBubble` 渲染 assistant content 前用 UUID 正则扫描
- 命中 → 异步 `getNote(id)` 查标题，缓存在 `ai-store.noteMetaCache`（LRU 100 条）
- 替换为 pill 组件：左键 → `openNoteById(id) + navigate('/')`；右键菜单 → 复制 id
- 失败（笔记不存在 / 已软删除）→ 渲染成灰色 `{id}` 代号 + tooltip "笔记已删除或不存在"

**升级到方案 B 的触发条件**（写进 plan 作为回退预案）：

- AI 开始频繁渲染非笔记 UUID（误触率 > ~10%）
- 用户反馈标题 stale（笔记重命名后已有 bubble 不更新）

方案 B = daemon agent-loop 跟踪本轮 tool 调用涉及的 `referenced_note_ids`，SSE 结构化下发。

### 7.5 P3.4-d 标签栏输入框 Tab/Enter 区分

**Scope 仅限编辑区标签栏输入框（`TagBar` 组件）**，不改 CodeMirror 编辑器正文。

当前行为：回车强制进入选择器（对 `/time` / `/alarm`），没有"只补全字面量"的路径。

**新行为**：

- **Tab**：补全高亮项的**字面量前缀**（如 `/ti` → `/time `），光标停在字面量后面，用户可继续打 `19:00`；**不触发 picker**
- **Enter**：补全 + 提交 / 触发 picker（现有行为保持）
- 候选源：`tag-store`（`#tag`）+ 静态字面量（`/time ` / `/alarm `）
- Esc 关 popup

**不做**：

- `[[` note-link 补全（owl 无 wikilink 语法，用户 2026-05-04 明确放弃）
- 编辑器正文内的 slash command 触发器
- 频繁短语补全 / 语料库挖掘

### 7.6 P3.4-e 笔记 tab VSCode 风格

按原 #5 scope：

- 单击列表：预览打开（tab 标题斜体 + 下划点线），点下一条自动替换当前预览
- 双击列表：固定（tab 标题正常），不会被下一次预览替换
- 已修改 / 已保存的 tab 不受影响
- 编辑器任意输入 / 脏标一旦出现 → 自动升为固定

状态字段：`EditorTab.preview: boolean`（新增），默认 true，双击或脏标触发时改 false。

### 7.7 P3.4-f 聊天持久化 + 侧栏布局

按现有 plan `docs/plans/2026-04-18-chat-persistence.md` 执行，不重复列。执行前先复核该文档是否与当前 `ai-store` / `ConversationStore` 实现对齐（P2-8 之后有改动）。

### 7.8 P3.4 完成定义

- 6 个子项全 ✅
- 每个子项动工前单独开 design doc（复用 P3.2-a/b/c/d 命名：`docs/plans/YYYY-MM-DD-p3-4-<letter>-<name>-design.md`）
- `just check` + `just test` 全绿
- 手动测试清单按 owl `CLAUDE.md` 规范出，用户签字
- 完成后不立刻发版；P3.4 累计改动进入 0.4.0（和 P4 migration 一起发）

### 7.9 不在 P3.4（明确移至 P6）

| 项 | 归属 |
|---|---|
| macOS menubar tray 图标 + 设置 toggle | P6 |
| 图片复制粘贴 + 缓存目录 + 自动链接（原 #1） | P6 |
| FIM 上下文补全 + Tab 补全（原 #8） | P6 |
| `append_memo` 语义（特殊 id vs `#memo` 标签） | P6 |
| AI 草稿 banner-instead-of-overwrite（option C） | P6 |
| `[[` note-link 补全 + 渲染 + 反向链接 | P6+（需先定 wikilink 语法，显式放弃 P3.4 做） |
| 编辑器正文 slash command 触发器 | P6 |

---

## 8. P4 — migration 同步

> **⚠️ 本节已作废（2026-05-07）**。P4 方向已从"rclone bisync 文件级同步"改为 **skybridge local-first operation-log 同步**。原 `migration/` 仓重命名为 `skybridge/`。新路线见：
> - 架构框架：`aviary/docs/SKYBRIDGE_ARCH.md`
> - owl 端对接计划：`docs/plans/2026-05-07-p4-skybridge-plan.md`
> - 进度追踪：`PROCESS.md`
>
> 以下原文保留仅作历史参考。

核心：用 TypeScript 重写 rclone bisync 同步工具（repo 根 `migration/` 子项目从 Go 迁 TS）。

**范围**（详细 plan 进入 P4 时再开）：

- 同步范围表见 orpheus-aviary 根 `CLAUDE.md`（aviary_config / 各工具 config / lark songs.db / owl.db）
- `owl.sync.db` 作为 owl.db 的同步副本，应用不直接读写
- 远程 daemon 连接（原 P2-1 范围，与 migration 同步语义耦合）
- 触发方式：手动 + 定时 + 冲突报告
- Semantic search / embeddings 放这里评估（FTS 质量足够则不做）

**依赖**：P3.4 完成后才进，因为 P3.4-a 改了 schema，migration 逻辑要在 `user_version >= 2` 语境下写。

**交付版本**：P3.4 + P4 一起发 `0.4.0`。

---

## 9. P5 — 打包 / 发布自动化

把原 §10 post-P3 候选集里的"打包 / 发布"部分一次性收进这里做完。

**范围**：

- GitHub Actions `release-gui.yml`（macOS + Windows + Linux 三矩阵）
- GitHub Actions `release-cli.yml`（npm publish 自动化）
- Windows `nsis` installer + `.ico` 图标
- Linux `AppImage` + `.png` 图标
- macOS codesign / notarize（届时若有 Apple Developer 账号）
- `electron-updater` + GitHub Releases provider（应用内自动更新）
- 单二进制 CLI（`@yao-pkg/pkg` 处理 `better-sqlite3` .node）
- GUI installer ↔ CLI 联动：installer 可选复制 `owl` 到 PATH / CLI 可 `owl install-gui` 拉 Release installer（跨平台静默安装差异单独评估）
- macOS universal（x64 + arm64）按需

**交付目标**：用户一条命令装完整套（任一入口都引导另一个），Windows / Linux 一等公民。

---

## 10. P6 — 次要功能集

非核心、放到 packaging 稳定之后再做。具体 scope 届时再敲定，此处仅保留记录。

| 项 | 来源 |
|---|---|
| macOS menubar tray 图标 + 设置 toggle | 原 P3.1 移出 |
| 图片复制粘贴 + 缓存目录 + 自动链接 | 原 #1 / P3.0.5 移出 |
| FIM 上下文补全 + Tab 补全 | 原 #8 |
| `append_memo` 语义改造（`#memo` 标签 vs 特殊 id） | 原 P2 deferred |
| AI 草稿 banner-instead-of-overwrite（option C） | 原 P2-8 deferred |
| `[[` note-link 语法 + 补全 + 渲染 + 反向链接面板 | 原 P3.4 deferred |
| 编辑器正文 slash command 触发器 | 原 P3.4 deferred |
| `owl doctor --recover`（rebuild 失败自动恢复） | 原 §10 |
| `owl doctor` GUI hint | 原 §10 |
| `owl s` 等 CLI 命令别名 | 原 §10 |
| `@owl/core` 公开发布 | 原 §10 |
| 0.3.0 rebuild 分支代码移除 | 0.4.0 清理项 |

---

## 11. P3.5（可选） — MCP server

`@orpheus-aviary/owl-mcp`：把现有 tool registry 包装成 MCP server，供 Claude Desktop / Cline / 其他 host 接入。

- 复用 `packages/daemon/src/ai/tools/` 全部现成工具
- 新建 `packages/mcp/`，依赖 `@modelcontextprotocol/sdk`
- 独立发包到 npm
- 运行时：可选 stdio 模式（spawn by host）或 HTTP 模式（独立服务）

**延后到 P3 后期**：先看 CLI 在外部 agent 场景是否已经够用。

---

## 12. 风险与未决

### 12.1 已知风险

- **macOS 未签名**：Gatekeeper 弹窗可能劝退部分用户。早期可接受，有投诉再补签
- **better-sqlite3 prebuild 覆盖**：非主流 Node 版本 / 架构落到本地编译。`owl doctor` 负责检测 + 给明确错误
- **rebuild 迁移中断**：断电 / 进程 kill → 原库仍在，`.new` 和 `.backup` 需手动清理或 `owl doctor --recover`（移至 P6）
- **publishable manifest 与 workspace manifest 偏差**：CI 脚本必须保证两者 dependencies 一致，否则 npm install 会缺依赖
- **P3.4-a fractional indexing 精度耗尽**：极端连续同位置插入会使 `position` 浮点精度归零，需 lazy rebalance 兜底

### 12.2 历史已解决

- ~~CLI 发包路径~~ → §5.3 tsup bundle + external native，生成独立 publishable manifest
- ~~P3.2 / P3.3 CLI 交付形态矛盾~~ → installer 集成 CLI 移至 P5
- ~~migration runner 选型 + 0001 语义~~ → 手写 migration + rebuild 策略（§5.5）
- ~~npm scope rename 边界~~ → 仅 rename 对外 CLI，内部 `@owl/*` 保留
- ~~CLI 命令命名别名~~ → P3.3 保持 full name，别名移至 P6
- ~~rebuild 失败恢复 UX~~ → P3.3 保留手动引导，`owl doctor --recover` 移至 P6
- ~~Tag 策略~~ → P3.3 落地 `v* = GUI` / `cli-v* = CLI`
- ~~P3.4 scope~~ → 2026-05-04 敲定 6 子项（§7）+ 分层 P4/P5/P6

### 12.3 未决项

- `open_note_in_gui` SSE 通道鉴权（localhost-only 已够？）— 当前 `/events` 路由无鉴权，外网不可达但本机多用户场景未处理
- **P3.4-a DnD 交互细节**：跨组拖动（置顶 → 非置顶）是否需要二次确认？gap drop target 视觉 hint 形式？→ P3.4-a design doc 敲定
- **P3.4-c regex 边界**：AI 偶尔输出非笔记 UUID 的误匹配率阈值？目前定 10%，实际跑起来看 → P3.4-c design doc
- **P4 migration 冲突 UX**：bisync 冲突时双端都改过的 note，冲突报告形式（类似 git merge？）→ P4 开工时敲定

---

## 13. 0.3.1 短期候选（非分层阶段）

不属于 P3.4/P4/P5/P6 任一阶段的小 polish，可随时 pick up：

- **`repository.url` canonical 化**：`apps/cli/scripts/gen-publishable-manifest.mjs` 产出 `https://github.com/orpheus-aviary/owl`，npm publish 时被 normalize 成 `git+https://.../owl.git`，下次 publish 前在 script 里直接写 canonical 格式消掉 warn
- 任何发版后用户反馈的 P0/P1 bug 修复

---

## 14. 参考文档

- P2 设计文档：`docs/plans/2026-04-12-p2-design.md`
- P2-7 AI 实施：`docs/plans/2026-04-17-p2-7-ai-implementation.md`
- 聊天持久化（P3.4-f 子项）：`docs/plans/2026-04-18-chat-persistence.md`
- P3 延后集：`docs/plans/p3-deferred.md`（原始素材，本文档取代）
- 主 COEDIT 计划：`docs/plans/COEDIT_PLAN.md`
