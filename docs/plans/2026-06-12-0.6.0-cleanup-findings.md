# Step 0 cleanup findings (0c) — post-refactor scan

> 扫描 2026-06-12，**在 Step 0 重构（0a 平台适配 + 0b owl-shared + fetch-SSE，commit `1a5e561`/`b190a4c`/`0c5d1ac`）之后**。
> 范围 = **轻量 cleanup**（决策 3）：warnings 清零 + 死 export + 文档/依赖；`>500` 行大文件拆分**只登记不做**。
> 流程：本清单 → 与用户 triage → 0d 分批执行（每批 `just check` + `just test` 全绿、不夹带功能、commit 前问用户）。
> 基线：core 528 / daemon 290 / cli 137 / gui 406 + gated e2e 25；`just check` 11 守卫；53 biome warnings。

## 维度 1 — biome warnings（53）

| 规则 | 数量 | 分布 | 0d 处置 |
|------|:--:|------|------|
| `noNonNullAssertion` | 32 | **28 在测试**（`sync-auth.test` 14 / `NoteList.test` 8 / `coalesce.test` 6）+ **4 在 src** | 轻量可清 |
| `noExcessiveCognitiveComplexity` | 20 | 散在 src 大函数（`TagBar` / `ChatSidebar` / `DraftReadyCard` / `engine` / `migrate` 等） | **需拆函数 = 延后**（与大文件绑） |
| `noDelete` | 1 | 1 处 `delete obj.x` | 轻量可清（→ `obj.x = undefined` 或重建对象） |

- **noNonNullAssertion**：src 的 4 处逐个 `!` → `?.` / 显式 narrow；测试里的 28 处——`x!` 在测试里是常见惯用法，**逐个改 narrow 噪声大**，建议改用 **测试目录 biome override**（`overrides: [{include: ["**/*.test.*"], rules: {style: {noNonNullAssertion: off}}}]`）一刀豁免（见 triage Q2）。
- **noExcessiveCognitiveComplexity（20）**：本质是大函数复杂度，**不属轻量**——要么拆函数（= 延后的大文件工作），要么 `biome-ignore` 逐条豁免+注明，要么调阈值。见 triage Q1。

## 维度 2 — 死 / 暂留 exports

| 符号 | 现状（精确核对） | 处置 |
|------|------|------|
| `writeSkybridgeConfig` | 零生产调用方，但 **`daemon/sync.test.ts` 经 `@owl/core` index 跨包用**（daemon 测试拿不到 core 内部相对路径） | **保留**（de-export 会断 daemon 测试；0d 实测发现） |
| `requireAuth` | **零生产调用方 + 唯一消费者是 `core/config.test.ts`（经 `./config.js` 直接 import）** | **已降级**（0d：从 index 去掉，测试不受影响） |
| `clearSkybridgeAuth` | **生产在用**（`gui/main/sync-auth.ts:370,659`） | **保留** |
| `SKYBRIDGE_NOT_INSTALLED` | `daemon/session.ts:154` 错误码 + `cli/errors.ts:33,77`；npm dep 是硬依赖 → 分支不可达 | **保留**（廉价防御，注明 unreachable）或删，见 triage Q3 |

> 注：0.6.0-plan 与 Step 0 草稿一度记 `writeSkybridgeConfig`/`requireAuth` 为「GUI main + 测试仍用」——**核对后 GUI main 不用**，是 test-only，可降级。

## 维度 3 — `>500` 行源文件（**登记，0d 不做**）

`sync-auth.ts` 1042 · `engine.ts` 1040 · `notes/index.ts` 754 · `FolderPanel.tsx` 753 · `editor-store.ts` 698 · `skybridge/config.ts` 692 · `migrate.ts` 656 · `ai-store.ts` 601。
拆分风险大、不阻塞 web，**显式延后**到 Step 0 之后单排一轮（与维度 1 的 20 个复杂度 warning 同源）。

## 维度 4 — 重复建模

- `SyncStatusResult`：owl-shared（新）+ `daemon/sync/manual.ts`（源）—— **有意 mirror**（daemon 是 Node、owl-shared 是 browser-lib，跨边界不直接共享），非可删重复。
- `RunSyncResult`：`gui/src/shared/sync-run-types.ts` mirror 自 `core/sync/engine.ts` —— 同上，有意 mirror。
- **结论**：当前 mirror 是边界刻意取舍，不在轻量范围动；Phase A 若让 daemon 也消费 owl-shared 类型再议。**0d 不做。**

## 维度 5 — 测试冗余

未发现明显可合并/重复 fixture。gated e2e 与单测覆盖分工清晰。**0d 无项。**

## 维度 6 — 文档冗余

- `docs/plans/` 有 ~60 个设计文档，**P1–P5 大量已 ship 的子设计未归档/未在 `docs/history/*-shipped.md` 建索引**。
- 已知作废：`COEDIT_PLAN.md`、`2026-04-20-p3-plan.md`（§8 作废，PROCESS.md 已注）。
- **处置**：轻量做法 = 在 history 索引补一段「P1–P3 历史设计稿清单」指针 + 给 `COEDIT_PLAN.md`/`p3-plan.md` 顶部加「已作废/已归档」标注；**不物理移动**（移动会断历史引用）。见 triage Q3。

## 维度 7 — 依赖审计

- CLI `commander` **在用**（1 import）；**无 `pkg` 依赖**（0.6.0-plan 的担忧不成立，build 是 `tsup`）。
- owl-shared **零运行时依赖**（纯 fetch/类型），干净。
- workspace 版本对齐 OK（6 包，pnpm install 通过）。**0d 基本无项**（顶多删 0 个未用 dep）。

## 维度 8 — TODO / FIXME

src 实质 **0**（21 命中全是 `SPECIAL_NOTES.TODO` / node_modules 误报）。**0d 无项。**

---

## triage 建议（待你定）

**0d 轻量批次（建议做）**：
- **批 1**：noNonNullAssertion 清零（src 4 处逐改 + 测试 28 处走 biome override）+ noDelete 1 处。
- **批 2**：降级 `writeSkybridgeConfig` / `requireAuth`（test-only）；`SKYBRIDGE_NOT_INSTALLED` 定性（保留+注明）。
- **批 3**（可选）：文档 hygiene——history 索引补 P1–P3 历史稿指针 + COEDIT/p3-plan 顶部标注。

**显式延后（不进 0d）**：20 个 `noExcessiveCognitiveComplexity` + 8 个 `>500` 行大文件 + 类型 mirror dedup —— 同源，另排一轮重构。

## 待你拍板的 triage 问题

- **Q1（复杂度 20 warning）**：① 测试目录豁免不解决它们（它们在 src）；选 (a) 延后留着当 warning / (b) 逐条 `biome-ignore`+注明 / (c) 调高阈值豁免。**倾向 (a) 延后**（与大文件一起重构时自然消解）。
- **Q2（测试里的 28 个 `!`）**：选 (a) 加测试目录 biome override 一刀豁免 / (b) 逐个改 narrow。**倾向 (a)**（`!` 在测试是惯用法，逐改噪声大）。
- **Q3（文档 + SKYBRIDGE_NOT_INSTALLED）**：文档 hygiene 做到「索引+标注」还是这轮先跳过？`SKYBRIDGE_NOT_INSTALLED` 保留还是删？**倾向**：文档轻量标注；错误码保留。
