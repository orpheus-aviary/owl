# P2-8 AI Chat Page — Implementation Plan

## Context

P2-7 (a → e) shipped the daemon-side AI backend: agent loop, 13 tools (read + Tier-1/2 writes), SSE endpoint, draft/preview pipeline. P2-8 builds the GUI side: a chat page that streams agent responses, surfaces drafts and direct writes, and reconciles AI changes with whatever the user is editing.

Backend stays untouched. All work is in `packages/gui/`.

## Decisions (agreed 2026-04-17)

- **Conversation organization**: tab-style, multiple chats open at once (mirrors `EditorPage`). One backend `conversation_id` per tab.
- **Draft handoff**: manual. `draft_ready` renders a `DraftReadyCard` in the chat; user clicks "open" to spawn the editor tab.
- **Tier-1 conflicts (`note_applied` on dirty tab)**: auto-merge — append AI's text to the user's *current* content, refresh save baseline from DB, fire toast. No modal. AI loop is never blocked.
- **Tier-2 conflicts (`draft_ready` for `update` action)**: modal at **save time only** (not on arrival). Two choices: `accept AI` / `keep mine`, with a `view diff` button opening a split read-only view via `@codemirror/merge`. AI loop is never blocked. Successive AI updates to the same tab overwrite the previous `pendingAiUpdate`.
- **Conflict granularity**: unified — one decision covers content + tags + folder. No per-field UI.
- **Merge UX**: no three-way merge. Binary accept/reject + side-by-side diff for visual confirmation only.
- **Conversation persistence**: in-memory in daemon (per P2-7c). Closing a tab in the GUI also `DELETE /ai/conversations/:id`. Daemon restart clears everything; GUI shows that on next open.
- **External-agent preview UI**: out of scope for P2-8. CLI in P3 will handle `POST /ai/preview/apply` directly.

## File Structure

```
packages/gui/src/renderer/src/
├── pages/AIPage.tsx                # NEW — chat tabs + active conversation view
├── stores/ai-store.ts              # NEW — chat tabs, streaming state, abort controllers
├── lib/sse-client.ts               # NEW — fetch + ReadableStream SSE parser
├── lib/api.ts                      # MODIFY — add /ai/* endpoints (capabilities, conversations, preview/apply)
├── stores/editor-store.ts          # MODIFY (delta only — see "Existing scaffolding" below)
├── components/ai/
│   ├── ChatTabBar.tsx              # NEW — top tab strip, "+ new chat", close
│   ├── MessageList.tsx             # NEW — scrollable virtualised list, auto-scroll on append
│   ├── MessageBubble.tsx           # NEW — user / assistant text bubble (markdown via existing renderer)
│   ├── ToolCallBlock.tsx           # NEW — collapsible block showing tool name + args + result
│   ├── DraftReadyCard.tsx          # NEW — preview + "open in editor" button
│   ├── NoteAppliedToast.tsx        # NEW — toast for Tier-1 writes
│   ├── ChatInput.tsx               # NEW — textarea + send + abort buttons
│   └── ConflictDialog.tsx          # NEW — modal triggered by saveNote when baselines diverge
└── components/ai/diff/
    └── DiffView.tsx                # NEW — @codemirror/merge wrapper, read-only split view
```

## New Dependency

- `@codemirror/merge` — official CodeMirror 6 split/unified diff view. Zero peer-dep beyond what we already have. Add via `pnpm add @codemirror/merge --filter @owl/gui`.

---

## Component Slice 1 — SSE Client (`lib/sse-client.ts`)

**Goal:** Parse the daemon's `event: <name>\ndata: <json>\n\n` stream into typed `AgentEvent`s.

**Why custom (vs `EventSource`):** native `EventSource` only supports GET. Our `/ai/chat` is POST with a JSON body, so we use `fetch` + `ReadableStream` and parse the wire format ourselves.

```ts
export interface SseStreamOptions {
  url: string;
  body: unknown;
  signal?: AbortSignal;
  onEvent: (event: string, data: unknown) => void;
  onError?: (err: unknown) => void;
}
export async function streamSse(options: SseStreamOptions): Promise<void>
```

Implementation notes:
- Decode chunks with `TextDecoder`, buffer until two newlines, then split into individual events.
- Tolerate SSE comment lines (`:`) and blank padding.
- Surface `signal.aborted` as a normal completion (no error).
- Single error path: throws on non-2xx response, network errors propagate via `onError`.

Tests: golden-input parsing (multi-event, partial chunks, ignored fields), abort mid-stream.

---

## Component Slice 2 — `ai-store` (zustand)

**Shape:**

```ts
interface ChatMessage {
  id: string;             // local uuid; no server identity
  role: 'user' | 'assistant';
  content: string;        // accumulated text deltas
  toolCalls: ChatToolCall[];
  drafts: DraftReady[];   // draft_ready cards rendered inline
  isStreaming: boolean;   // assistant message that's still receiving deltas
}

interface ChatToolCall {
  id: string;             // server tool_call_id
  name: string;
  args: Record<string, unknown>;
  result?: unknown;       // populated when tool_result arrives
  isError?: boolean;
}

interface ChatTabState {
  id: string;             // local — used as React key; matches server conversation_id once known
  conversationId: string | null;  // null until first 'conversation_id' SSE event
  title: string;          // first user message truncated; "新对话" until then
  messages: ChatMessage[];
  pendingNoteApplieds: NoteAppliedNotice[]; // queued for the toast lane
  abortController: AbortController | null;
  isStreaming: boolean;
}

interface AiState {
  chats: ChatTabState[];
  activeChatId: string | null;

  newChat(): string;                            // returns local id
  closeChat(id: string): Promise<void>;          // also DELETEs server conversation
  setActiveChat(id: string): void;
  sendMessage(chatId: string, text: string): Promise<void>;  // wires SSE
  abortStreaming(chatId: string): void;
}
```

**Key flows:**

- `sendMessage`:
  1. Push optimistic user message + empty assistant placeholder (`isStreaming=true`).
  2. Open `AbortController`, store on tab.
  3. `streamSse({ url: '/ai/chat', body: { message, conversation_id }, ... })`.
  4. Per event, call dispatcher (next slice).
  5. On `done` or abort or error, clear `abortController` and `isStreaming`.

- `abortStreaming`: calls `controller.abort()`. SSE client exits cleanly; the assistant message is left as-is (partial), trailing toast: "已停止生成".

---

## Component Slice 3 — Event Dispatch

Single dispatcher inside `ai-store` consumes raw SSE events and decides what to do. Centralising it keeps the SSE client dumb and makes it testable in isolation.

| Event             | Action                                                                                                       |
|-------------------|--------------------------------------------------------------------------------------------------------------|
| `conversation_id` | Set `chat.conversationId`                                                                                    |
| `message`         | Append `content` to streaming assistant message                                                              |
| `tool_call`       | Push a new `ChatToolCall` onto current assistant message                                                     |
| `tool_result`     | Patch the matching `ChatToolCall.result` / `isError`                                                         |
| `note_applied`    | (a) Forward to `editorStore.applyNoteAppliedFromAi(...)` for in-tab merge; (b) push a toast notice           |
| `draft_ready`     | Push a `DraftReady` card onto current assistant message; **do not** auto-open the editor                     |
| `preview_ready`   | Push a card with "(external preview — open via CLI)" text. No GUI action — out of scope.                     |
| `error`           | Append a system-style error bubble; clear streaming flag                                                     |
| `done`            | Clear streaming flag                                                                                         |

---

## Existing `editor-store` scaffolding (DO NOT re-build)

P2-7e already shipped most of the AI-related state and actions. P2-8 only
*adds* on top — list to keep implementers from regressing what's there:

| Already in place                                            | Source                  |
|-------------------------------------------------------------|-------------------------|
| `TabState.isDraft / pendingAiUpdate / originalFolderId`     | `editor-store.ts:24-39` |
| `openAiDraft(draft: AiDraftInput)` action                   | `editor-store.ts`       |
| `stageAiUpdate(noteId, payload)` action                     | `editor-store.ts`       |
| `detectPendingUpdateConflict(tab, pending)` pure function   | `editor-store.ts`       |
| `saveNote` 3-branch path (POST draft / PATCH AI / PUT plain) | `editor-store.ts`      |
| `syncTabFolderId` updates both `folderId` + `originalFolderId` | `editor-store.ts`     |

What P2-8 needs to **add**:

1. `applyNoteAppliedFromAi(note_id, latestDbContent, appendedText)` — Tier-1 auto-merge action (Slice 4 below).
2. `conflictPrompt: { tabId, pending, conflict } | null` state field — what `ConflictDialog` reads.
3. `requestSaveOrConflict(noteId)` — small wrapper that the editor's Cmd+S handler calls instead of `saveNote` directly. Detects conflict on AI updates; if found, sets `conflictPrompt` and short-circuits. Otherwise delegates to `saveNote`.
4. `resolveConflict(decision: 'accept-ai' | 'keep-mine')` — applies the choice to the staged tab, clears `conflictPrompt`, then re-invokes `saveNote`.

Everything else in `editor-store.ts` is untouched.

---

## Component Slice 4 — Tier-1 Auto-Merge (`applyNoteAppliedFromAi`)

New action on `editorStore`:

```ts
applyNoteAppliedFromAi(note_id: string, latestDbContent: string, appendedText: string): void
```

Behaviour:

1. If no tab open for `note_id` → no-op (toast in `ai-store` is sufficient).
2. If tab is **not dirty** → silent overwrite: `tab.content = latestDbContent`; baselines reset to `latestDbContent`.
3. If tab is **dirty** → auto-merge: `tab.content = tab.content + '\n\n' + appendedText`; `tab.originalContent = latestDbContent` (the new save baseline reflects what the DB now has). `dirty` stays `true`.

Rationale: case 3 means the user's next `Cmd+S` will diff their merged content against the DB-with-AI-append baseline; their save still goes through correctly via the existing `updateNote` path. The user sees both their edits *and* AI's append; if they don't want it, undo (CodeMirror history is preserved because we append at end rather than re-setting the whole doc).

Edge case: AI fires a second `note_applied` while the tab is still dirty — same logic applies, append again. No coalescing needed.

---

## Component Slice 5 — Conflict Dialog (`ConflictDialog.tsx`)

**Trigger:** the editor's Cmd+S handler calls `editorStore.requestSaveOrConflict(noteId)` (new wrapper). If the tab has a `pendingAiUpdate` AND `detectPendingUpdateConflict(...)` reports any `true`, the wrapper sets `conflictPrompt` and returns without saving — the dialog reads that state and renders. `saveNote` itself stays unchanged so direct callers keep the existing fast-path.

**Layout:**

```
┌─────────────────────────────────────────────────────┐
│ AI 对此笔记的修改与你本地的修改不一致                          │
│                                                     │
│ 冲突项: [content] [tags] [folder]                     │
│                                                     │
│ [ 接受 AI 版本 ]  [ 保留本地 ]  [ 查看差异 ]              │
└─────────────────────────────────────────────────────┘
```

Buttons map directly to `resolveConflict(decision)`:
- **接受 AI 版本** → `resolveConflict('accept-ai')` — overwrites the tab's content/tags/folderId with `pendingAiUpdate.{content,tags,folder_id}`, clears `conflictPrompt`, then calls `saveNote` (now conflict-free).
- **保留本地** → `resolveConflict('keep-mine')` — clears `tab.pendingAiUpdate`, clears `conflictPrompt`, calls `saveNote` (normal PUT path).
- **查看差异** → opens `DiffView` modal (split view, AI version on right, current local on left, both read-only). Below the diff, the same two buttons reuse `resolveConflict`.

**Where the dialog lives:** rendered globally at the editor page level, driven by `editorStore.conflictPrompt`. Not in AI page — the user might be in the editor when they hit Cmd+S.

---

## Component Slice 6 — `AIPage.tsx` Layout

```
┌─ AIPage ──────────────────────────────────────────┐
│ ChatTabBar (横向 tabs + "+" + close)                │
├───────────────────────────────────────────────────┤
│ MessageList                                       │
│  ┌ user bubble ──────────────────────────────┐    │
│  │ "帮我写一个会议笔记模板"                       │    │
│  └───────────────────────────────────────────┘    │
│  ┌ assistant bubble ─────────────────────────┐    │
│  │ "好的,我来创建一个..."                       │    │
│  │  ▸ tool_call: create_note ({...})         │    │
│  │  ▸ tool_result: {...}                      │    │
│  │  ┌ DraftReadyCard ──────────────────┐     │    │
│  │  │ 📝 创建笔记 "会议模板"              │     │    │
│  │  │ #会议 #模板    folder: (root)       │     │    │
│  │  │           [打开]                   │     │    │
│  │  └─────────────────────────────────┘     │    │
│  │ "已为你准备好,点击打开后保存即可。"            │    │
│  └───────────────────────────────────────────┘    │
├───────────────────────────────────────────────────┤
│ ChatInput (textarea + send/abort)                 │
└───────────────────────────────────────────────────┘
```

- Empty state: centred prompt suggestions ("你可以问我..." with 3-4 example chips; clicking fills input).
- Markdown in assistant bubbles uses the existing `MarkdownPreview` component (`components/MarkdownPreview.tsx`).
- Tool call blocks default collapsed (just `▸ tool_name`); click to expand args/result JSON in a `<pre>`.
- Auto-scroll to bottom when new content arrives, but only if user is already near the bottom (otherwise show "↓ N new" pill).

---

## Implementation Order (sub-phases)

| # | Slice | Dep | Verification |
|---|-------|-----|--------------|
| 1 | `lib/sse-client.ts` + parser tests (vitest) | none | unit tests pass |
| 2 | `lib/api.ts` /ai/* endpoints + minimal `ai-store` skeleton | 1 | typecheck |
| 3 | `AIPage.tsx` empty shell + ChatTabBar + new-chat action | 2 | manual: tabs render, can add/remove |
| 4 | ChatInput + sendMessage wiring + MessageBubble + streaming text rendering | 3 | manual: type message → see streaming reply |
| 5 | ToolCallBlock + DraftReadyCard rendering | 4 | manual: ask AI to call a tool / draft a note |
| 6 | `editorStore.applyNoteAppliedFromAi` + NoteAppliedToast | 4 | unit tests for merge logic + manual |
| 7 | DraftReadyCard "open" → `editorStore.openAiDraft` (existing) | 5, 6 | manual: open → save → tab persists |
| 8 | Pull `@codemirror/merge`; build `DiffView` | none | renders two strings |
| 9 | ConflictDialog + `editorStore.saveNote` integration | 6, 8 | unit tests on detection + manual flow |
| 10 | Polish: empty state, scroll, keyboard shortcuts, abort UI, error bubbles | 1-9 | manual end-to-end |

Each numbered step is committable on its own and in dependency order.

---

## Manual Testing Plan (per step + final)

Final E2E:

1. New chat tab; ask "列出我所有标签" → expect `list_tags` tool call block + tag list in assistant text.
2. Ask "提醒我明天 10 点买牛奶" → expect `create_reminder` tool call + DraftReadyCard with `/alarm` tag visible.
3. Click "打开" → editor opens with draft tab, content + tags pre-filled, dirty + isDraft.
4. Cmd+S → tab persists with real id, sidebar refreshes, AI page still active.
5. In another tab, edit memo (don't save). In AI tab, ask "记一下:测试自动合并". Expect: memo tab's content gains AI's text appended, toast appears, AI loop continues normally.
6. Open a real note tab, edit it. In AI tab, ask AI to update that same note. Cmd+S in editor → conflict dialog appears. Click "查看差异" → diff view shows both versions. Choose "接受 AI" → DB has AI version.
7. Close a chat tab → `DELETE /ai/conversations/:id` fires; `GET /ai/conversations` no longer lists it.
8. Restart daemon (`just stop-daemon` then `just dev-daemon`) → reopen GUI → AI page shows fresh-empty tabs (no zombie conversation ids).

---

## Out of Scope (explicit non-goals)

- Multi-user / multi-device chat sync (all in-memory per daemon).
- Conversation export / download.
- Streaming function-call argument display (we wait until args complete then render).
- Custom system-prompt editing UI (still in TOML config).
- External-agent preview UI in GUI (CLI-only via P3).
- Token usage / cost telemetry display.
- Rich attachments (images, files) — text only.

---

## Risks

1. **CodeMirror merge bundle size.** `@codemirror/merge` adds ~30-40 kB. Acceptable for an Electron app; revisit only if it shows up as a bottleneck.
2. **SSE parsing edge cases.** Multi-line `data:` payloads, comment lines, BOM. Tests cover golden inputs but real LLM streams may surprise — add fallback "skip unknown line" behaviour, log to console.
3. **AI loop racing user input.** While the user is reading a streaming reply they may type the next message. Solution: input is always enabled but `Send` button disabled while `isStreaming`. Add an `Abort` button that fires `controller.abort()`.
4. **Conflict dialog timing.** If user hits Cmd+S faster than the SSE event for `draft_ready` arrives, no conflict (state hasn't been staged yet). That's fine — they're saving their pre-AI work normally. Document but don't engineer around it.
5. **Tab close mid-stream.** Concrete order in `closeChat(id)`:
   1. If `tab.abortController` exists → `controller.abort()`.
   2. **Await** the in-flight `streamSse` promise (it resolves on abort).
   3. If `tab.conversationId != null` → `await api.deleteConversation(tab.conversationId)`.
   4. Remove the tab from state, pick a new `activeChatId`.

   Doable thanks to the `conversation_id` event arriving as the FIRST SSE event (P2-7c, see plan agent-events list), so even mid-stream we already know the id. If `conversationId` is still null (server never responded), skip step 3 — there's nothing to delete.

---

## E2E 手动测试清单（shipped 2026-04 期间）

跑 `just dev` 之后按顺序验证：

### A. SSE + 基础对话
1. 编辑页随便建一条笔记 → 切到 AI 页（侧栏 Cmd+6）
2. 第一次进去应自动建 **1** 个 chat（不是 2 个）
3. 输入"列出我所有标签"，按 Enter 发送
4. 预期：流式文字出现；tool_call `list_tags` 折叠块可展开看 args/result；结尾"思考中…"消失

### B. Tier-1 auto-merge（append_memo）
5. 编辑页打开 `#随记` 笔记（干净不脏）
6. 切到 AI 页，"在 memo 末尾追加 milk"
7. 预期：右上角绿色 toast "AI 已更新笔记"；memo tab 内容自动更新；DB `sqlite3 owl.db "select content from notes where id like '00000000%1'"` 可查到

### C. Tier-1 dirty-merge
8. 回到编辑页在 `#随记` tab 里再手写一行（不保存）→ tab 脏
9. AI 页 "在 memo 追加 eggs" → 预期：toast + memo tab 内容变成"(用户本地) + eggs"，脏标志仍在；Cmd+S 走 PUT 路径保存
10. DB 应有 milk + eggs 都在

### D. Tier-2 create 草稿
11. AI 页 "帮我创建一个叫'旅行清单'的笔记，内容写三项"
12. 收到 DraftReadyCard → 点"打开"
13. 预期：切到编辑页，新 tab 标题"旅行清单"，内容预填，Tab 脏，AI 页按钮变"已打开"
14. Cmd+S → POST 保存，Tab id 从 `draft_` 换成真实 UUID；笔记列表刷新

### E. Tier-2 update（无冲突）
15. 挑一条已有普通笔记（比如上面建的"旅行清单"），关掉它的 tab
16. AI 页 "把旅行清单的第三项改成'买保险'"
17. 收到 DraftReadyCard action=update → 点"打开"
18. 预期：笔记打开，内容是 AI 版本，Tab 脏，`pendingAiUpdate` 已 stage
19. Cmd+S → PATCH 保存，不弹冲突，Tab 干净

### F. Tier-2 update（触发冲突）
20. 打开"旅行清单"笔记，手动在末尾加一行 → Tab 脏
21. **不切走**，继续在同一笔记中停留；切 AI 页 "把旅行清单的第二项改成'订机票'"
22. 收到 DraftReadyCard → 点"打开"
23. 回到编辑页看到内容被 AI 覆盖
24. Cmd+S → **ConflictDialog 弹出**，"冲突项：内容"
25. 点"查看差异" → 左栏你的本地版（含刚加的那行）、右栏 AI 版
26. 点"保留本地" → tab 回滚到你的本地版 + 保存
    重复一次选"接受 AI 版本" → tab 保留 AI 版 + 保存，dialog 不再弹

### G. Abort UI
27. AI 页发一条长问题 "写一篇 500 字散文"
28. 流式到一半点 ⏹ Stop 按钮
29. 预期：光标消失，bubble 底部显示"⏹ 已停止生成"灰字
30. 输入框重新获得焦点可继续输入

### H. 聊天 tab 切换 + scroll 保留
31. 起两个 chat tab，各发几条消息
32. 在 tab A 滚动到中间位置 → 切 tab B → 切回 tab A
33. 预期：scrollTop 保留在你离开时的位置，不重置到顶

### I. 页面离开后再回
34. 在 AI 页发消息中途 Cmd+1 切编辑页
35. 切回 AI 页 → 预期：流式继续（后台运行），切走期间新增的 tool_call / 消息都在

### J. 删除系统笔记保护
36. 浏览页/编辑页找到 `#随记`，右键 → 移到回收站
37. 预期：弹"系统笔记无法删除"对话框（非静默失败）

### K. daemon 500 诊断
38. 手动停 daemon；GUI 调用任何 API 时出错会在 daemon.log 留 `unhandled route error` 条目含 stack（非 Fastify 默认的空）

## P2-8 实施进度（10 步全 shipped）

| Step | 内容 |
|------|------|
| 1 | `lib/sse-client.ts` + 9 vitest 测试 |
| 2 | `lib/api.ts` `/ai/*` wrappers + `ai-store` skeleton（newChat/closeChat/sendMessage） |
| 3 | `ai-dispatcher.ts` 纯函数 + 14 个事件测试（9 类事件全覆盖 + malformed/unknown） |
| 4 | AIPage shell + ChatTabBar + MessageList + MessageBubble + ChatInput |
| 5 | ToolCallBlock + DraftReadyCard + PreviewReadyCard + 嵌入 MessageBubble |
| 6 | `editorStore.applyNoteAppliedFromAi` + NoteAppliedToast（store onEvent wrapper 触发） |
| 7 | DraftReadyCard "打开" → `editorStore.openAiDraft / stageAiUpdate` wiring |
| 8 | `@codemirror/merge` 集成 + DiffView 组件 |
| 9 | ConflictDialog + `editorStore.requestSaveOrConflict / resolveConflict` |
| 10 | Polish: empty-state、scroll、shortcut、abort UI、error bubble、E2E manual test |
